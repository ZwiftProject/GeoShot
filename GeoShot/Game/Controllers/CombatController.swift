import SpriteKit

final class CombatController {
    private weak var scene: GameScene?

    init(scene: GameScene) {
        self.scene = scene
    }

    func update(deltaTime: TimeInterval) {
        // placeholder for combat updates
    }
}
