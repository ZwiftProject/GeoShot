import SpriteKit

final class DungeonController {
    private weak var scene: GameScene?

    init(scene: GameScene) {
        self.scene = scene
    }

    func update(deltaTime: TimeInterval) {
        // placeholder for dungeon/run orchestration
    }
}
