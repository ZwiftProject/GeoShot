import SpriteKit

final class RunController {
    private weak var scene: GameScene?

    init(scene: GameScene) {
        self.scene = scene
    }

    func startRun() {
        // placeholder
    }
}
